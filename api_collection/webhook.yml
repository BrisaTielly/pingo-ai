info:
  name: webhook
  type: http
  seq: 1

http:
  method: GET
  url: http://127.0.0.1:8001/webhook?hub.challenge=1234&hub.mode=subscribe&hub.verify_token=teste1
  params:
    - name: hub.challenge
      value: "1234"
      type: query
    - name: hub.mode
      value: subscribe
      type: query
    - name: hub.verify_token
      value: teste1
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
